/**
 * ===========================================================================
 *  CONTEÚDO DO SITE — Michelle Pedro · Psicóloga
 * ===========================================================================
 *
 *  Os textos, imagens e links agora são editados pelo PAINEL (TinaCMS), que
 *  grava em `src/content/site.json`. Este arquivo apenas reexporta esse
 *  conteúdo para os componentes (que seguem usando `import { site } from
 *  "@/content/site"`).
 *
 *  Para editar sem código, use o painel em `/admin`.
 * ===========================================================================
 */
import siteData from "./site.json";

export const site = siteData;
